{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "nixpacks"
  },
  "deploy": {
    "startCommand": "cd website && python3 server.py",
    "volumes": [
      {
        "name": "data",
        "mountPath": "/app/data"
      }
    ]
  }
}
